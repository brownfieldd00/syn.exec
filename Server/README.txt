Make sure you have (Node JS) installed on your system.

=== SETUP ===

Install the following packages: node-cmd and express.

(Run):
npm install node-cmd
npm install express

To deploy:
run (deploy_server.cmd)